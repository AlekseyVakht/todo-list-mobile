import { View, StyleSheet } from "react-native";

export default function BurgerMenu() {
  return <View stlye={styles.container}></View>;
}

const styles = StyleSheet.create({
    container: {
        
    }
});
